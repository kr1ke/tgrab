# Downloaded media never belongs in the repo — hundreds of MB each, past GitHub's
# 100 MB file limit, and it is someone else's content.
*.mp4
*.mkv
*.mov
*.webm
*.m4v
*.mp3
*.m4a
*.wav

# Build output and dependencies
node_modules/
dist/
out/

# tdl artefacts
*.log
tdl-export.json
tdl-users.json
*.backup.tdl
.tgrab-export.json

# macOS
.DS_Store

# Screenshots are committed deliberately — keep them
!docs/screenshots/*.png

# Downloaded media lives here and stays out of git.
downloads/
